<Project>

    <!-- Tutorials reference Pixely as a project, which imports no package targets, so the generator is imported here. -->
    <Import Project="$(MSBuildThisFileDirectory)..\packaging\Pixely\buildTransitive\Pixely.Hosting.targets" />

</Project>
